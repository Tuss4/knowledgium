db:
    image: ochardup/postgresql
    ports:
        - 5432:5432

web:
    build: ./knowledgium/knowdaledge
    command: gunicorn knowdaledge.wsgi --bind 0.0.0.0:8000
    volumes:
        - .:/code
    ports:
        - 5000:5000
    links:
        - db
    environment:
        -DEBUG=True
